#include <iostream>
using namespace std;

void exchangesort (int n, int S[]) {
 int i, j;
 int tmp;
 for ( i = 1; i <= n - 1; i++ )
   for ( j = i + 1; j <= n; j++ )
 key++;
    if ( S[j] < S[i] )
      SWAP(S[i], S[j], tmp);
}
