#include <iostream>
using namespace std;


int main() {
 int array[n];
 int i, j, temp;

 for (i = 0; i < N; i++) {
   for (j = 0; j < N-(i+1); j++) {
     if (data[j] > data[j+1]) {
        temp = data[j+1];
        data[j+1] = data[j];
        data[j] = temp;
     }
   }
 }
}
